# Configuração do editor/assistente, não do projeto.
.claude/

# D16 + D30 — enunciado, anotações e todo o material de planejamento são de
# trabalho, não de entrega. Quem comunica com quem avalia é o README.
docs/
CLAUDE.md

# Segredos — o .env.example é o template versionado; o .env, nunca.
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Sujeira de editor
.*.kate-swp
*.swp
*~

# Toolchain local do sandbox onde o Claude roda comandos (uv + interpretadores).
# Não faz parte do projeto: quem clona instala o uv normalmente, como diz o README.
.tools/
